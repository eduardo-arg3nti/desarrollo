# Clona el repositorio si aún no lo has hecho
git clone https://github.com/eduardo-arg3nti/desarrollo.git

# Navega al directorio del repositorio
cd desarrollo

# Crea la carpeta utils
mkdir utils

# Crea un archivo README dentro (opcional pero recomendado)
echo "# Utilidades" > utils/README.md

# Agrega los cambios
git add utils/

# Haz un commit con los cambios
git commit -m "Agregar carpeta utils"

# Sube los cambios a GitHub
git push origin main
